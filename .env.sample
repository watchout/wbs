# Example environment variables for WBS

# Database connection
DATABASE_URL=postgresql://user:pass@localhost:5432/wbs

# Google OAuth credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT secret for session tokens
JWT_SECRET=change-this-secret

# Base URL of the running application (also allowed CORS origins for Socket.IO)
APP_BASE_URL=https://wbs.example.com

# Stripe billing
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# AI Cost Table (optional overrides, default values in aiCostConfig.ts)
# AI_COST_VOICE_INPUT=0.5
# AI_COST_TEXT_PARSE=0.1
# AI_COST_SCHEDULE_AI=0.0
# AI_COST_SUMMARY=0.3
# AI_COST_CONSULT=1.0

# CORS allowed origins (comma-separated, required for production)
# ALLOWED_ORIGINS=https://wbs.example.com,https://app.mielboard.jp

# LLM Provider API Keys (at least one required for AI Assistant)
OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_AI_API_KEY=AI...
