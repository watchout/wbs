name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit --fund=false
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/wbs?schema=public
      - name: Lint
        run: node --check server/index.js && node --check server/events.js
        continue-on-error: false
      - name: Type Check
        run: |
          echo "typecheck: skipped (no TS toolchain configured yet)"
        continue-on-error: false

  build:
    name: Build Verification
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit --fund=false
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/wbs?schema=public
      - name: Build
        run: node --check server/index.js && node --check server/events.js
        env:
          NODE_ENV: production

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json
      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit --fund=false
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/wbs?schema=public
      - name: Security Audit
        run: npm audit --audit-level=moderate
        continue-on-error: true

  ssot-compliance:
    name: SSOT Compliance Check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Check for forbidden patterns
        run: |
          echo "🔍 禁止パターンチェック中..."
          
          # スキーマ変更チェック（原則禁止。ただし構文修復のみ例外で許可）
          if git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -q "prisma/schema.prisma"; then
            echo "⚠️ prisma/schema.prisma の変更が検出されました（原則禁止）"
            echo "→ 構文修復のみ（例: 不正文字/マーカー除去）に限り許可します。差分を検査します…"
            
            DIFF=$(git diff -U0 origin/${{ github.base_ref }}...HEAD -- prisma/schema.prisma)
            ADDED=$(echo "$DIFF" | grep '^+' | grep -v '^\+\+\+' || true)
            REMOVED=$(echo "$DIFF" | grep '^-' | grep -v '^---' || true)
            
            # 例外許可: 先頭行の "~//" → "//" への修復、および単独 "~" 行の除去
            ALLOWED_ADDED_REGEX='^\\+// prisma/schema\\.prisma'
            ALLOWED_REMOVED_REGEX='^\\-~// prisma/schema\\.prisma|^\\-~$'
            
            if echo "$ADDED" | grep -v -E "$ALLOWED_ADDED_REGEX" | grep -q .; then
              echo "❌ エラー: schema.prisma に許可されていない追加行があります"
              echo "$ADDED"
              exit 1
            fi
            if echo "$REMOVED" | grep -v -E "$ALLOWED_REMOVED_REGEX" | grep -q .; then
              echo "❌ エラー: schema.prisma に許可されていない削除行があります"
              echo "$REMOVED"
              exit 1
            fi
            
            echo "✅ schema.prisma: 構文修復のみの変更として許可"
          fi
          
          # 生SQLチェック
          if git diff origin/${{ github.base_ref }}...HEAD | grep -q '\$queryRaw\|\$executeRaw'; then
            echo "❌ エラー: 生SQLクエリの使用が検出されました"
            exit 1
          fi
          
          echo "✅ 禁止パターンチェック: 合格"
