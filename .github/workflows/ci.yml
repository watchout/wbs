name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Type Check
        run: npx vue-tsc --noEmit
        continue-on-error: true

  build:
    name: Build Verification
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
        env:
          NODE_ENV: production

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install --ignore-scripts
      - name: Security Audit
        run: npm audit --audit-level=high
        continue-on-error: true

  ssot-compliance:
    name: SSOT Compliance Check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Check for forbidden patterns
        run: |
          echo "🔍 禁止パターンチェック中..."
          
          # 生SQLチェック（ci.yml自体を除外）
          RAW_SQL_COUNT=$(git diff origin/${{ github.base_ref }}...HEAD -- ':(exclude).github/' | grep -c 'queryRaw\|executeRaw' || echo "0")
          if [ "$RAW_SQL_COUNT" -gt "0" ]; then
            echo "❌ エラー: 生SQLクエリの使用が検出されました ($RAW_SQL_COUNT 箇所)"
            git diff origin/${{ github.base_ref }}...HEAD -- ':(exclude).github/' | grep -n 'queryRaw\|executeRaw' || true
            exit 1
          fi
          
          # organizationIdなしのクエリチェック（新規APIファイルのみ）
          NEW_API_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -E '^server/api/.*\.(ts|js)$' || true)
          if [ -n "$NEW_API_FILES" ]; then
            for file in $NEW_API_FILES; do
              if [ -f "$file" ]; then
                if grep -q "prisma\." "$file" && ! grep -q "organizationId" "$file"; then
                  echo "⚠️ 警告: $file にorganizationIdフィルタがない可能性があります"
                fi
              fi
            done
          fi
          
          echo "✅ 禁止パターンチェック: 合格"
